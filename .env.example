# =====================================================
# Home Server Environment Variables
# =====================================================

# =====================================================
# Traefik
# =====================================================
DOMAIN=yourdomain.com
TRAEFIK_DASHBOARD_PORT=8080  # Доступ в локальной сети (НЕ пробрасывать на роутере!)
# Сгенерировать пароль:
# echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
# Или онлайн: https://hostingcanada.org/htpasswd-generator/
# По умолчанию: admin / changeme
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$8kOQFgh2$$LhxXpEtBWEGWvZREjQK2M.

# =====================================================
# PostgreSQL (общая БД для всех приложений)
# =====================================================
POSTGRES_PORT=5432  # Доступ в локальной сети (НЕ пробрасывать на роутере!)
POSTGRES_ROOT_PASSWORD=your-secure-postgres-password
POSTGRES_USER=postgres
POSTGRES_DB=nextcloud

# =====================================================
# Applications
# =====================================================

# -----------------------------------------------------
# Nextcloud - Облачное хранилище
# -----------------------------------------------------
NEXTCLOUD_LOCAL_PORT=8081  # Доступ в локальной сети (НЕ пробрасывать на роутере!)
NEXTCLOUD_DOMAIN=home.yourdomain.com
POSTGRES_NC_DB=nextcloud
POSTGRES_NC_USER=nextclouduser
POSTGRES_NC_PASSWORD=your-secure-nextcloud-db-password

# -----------------------------------------------------
# Navidrome - Музыкальный стриминг-сервер
# -----------------------------------------------------
NAVIDROME_LOCAL_PORT=4533  # Доступ в локальной сети (НЕ пробрасывать на роутере!)
NAVIDROME_DOMAIN=music.yourdomain.com
# Путь к музыкальной библиотеке на хосте (измените на свой!)
NAVIDROME_MUSIC_PATH=/path/to/your/music

# -----------------------------------------------------
# qBittorrent - Торрент-клиент
# -----------------------------------------------------
QBITTORRENT_LOCAL_PORT=8082  # Веб-интерфейс (НЕ пробрасывать на роутере!)
# Путь для загрузок на хосте (измените на свой!)
QBITTORRENT_DOWNLOADS_PATH=/path/to/downloads
# Примечание: порт 6881 для торрентов нужно пробросить на роутере (TCP+UDP)

# === Добавьте свои приложения ниже ===

# WordPress (пример)
# WORDPRESS_DOMAIN=blog.yourdomain.com
# WORDPRESS_DB=wordpress
# WORDPRESS_DB_USER=wpuser
# WORDPRESS_DB_PASSWORD=your-secure-wordpress-db-password
